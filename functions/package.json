{
  "name": "youtube-cache-functions",
  "version": "1.0.0",
  "description": "Google Cloud Functions untuk cache video YouTube",
  "main": "refreshYoutubeCache.js",
  "scripts": {
    "start": "functions-framework --target=refreshYoutubeCache",
    "deploy": "gcloud functions deploy refreshYoutubeCache --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point refreshYoutubeCache --timeout 540s --memory 512MB"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/firestore": "^7.1.0",
    "axios": "^1.13.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
